import { AddFormComponent } from './add-form.component';

describe('AddFormComponent', () => {
  let component: AddFormComponent;

  beforeEach(() => {
    component = new AddFormComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
